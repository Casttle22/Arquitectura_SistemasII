{
  "name": "arquitectura-sistemasii-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @as5/api",
    "build": "npm run build -w @as5/api && npm run build -w @as5/web",
    "lint": "npm run lint -ws"
  }
}